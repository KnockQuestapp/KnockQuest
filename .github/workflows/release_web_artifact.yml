name: release-web-artifact

on:
  workflow_dispatch:
    inputs:
      flavor:
        description: Build flavor (staging or production)
        required: true
        default: production
        type: choice
        options:
          - staging
          - production
  push:
    branches:
      - clean-final
    tags:
      - v*

concurrency:
  group: release-web-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-web:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve flavor
        id: flavor
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            echo "value=${{ github.event.inputs.flavor }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=staging" >> "$GITHUB_OUTPUT"
          fi

      - name: Check public app configuration
        env:
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
        run: |
          test -n "$SUPABASE_URL" || { echo 'Set repository variable SUPABASE_URL'; exit 1; }
          test -n "$SUPABASE_PUBLISHABLE_KEY" || { echo 'Set repository variable SUPABASE_PUBLISHABLE_KEY'; exit 1; }

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

      - name: Build web artifact
        env:
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
        run: flutter build web --release --dart-define=APP_FLAVOR=${{ steps.flavor.outputs.value }} --dart-define=SUPABASE_URL="$SUPABASE_URL" --dart-define=SUPABASE_PUBLISHABLE_KEY="$SUPABASE_PUBLISHABLE_KEY"

      - name: Build android artifact
        env:
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ vars.SUPABASE_PUBLISHABLE_KEY }}
        run: flutter build apk --release --flavor ${{ steps.flavor.outputs.value }} --dart-define=APP_FLAVOR=${{ steps.flavor.outputs.value }} --dart-define=SUPABASE_URL="$SUPABASE_URL" --dart-define=SUPABASE_PUBLISHABLE_KEY="$SUPABASE_PUBLISHABLE_KEY"

      - name: Upload web artifact
        uses: actions/upload-artifact@v4
        with:
          name: knockquest-web-${{ steps.flavor.outputs.value }}-${{ github.run_number }}
          path: build/web
          if-no-files-found: error

      - name: Upload android artifact
        uses: actions/upload-artifact@v4
        with:
          name: knockquest-android-${{ steps.flavor.outputs.value }}-${{ github.run_number }}
          path: build/app/outputs/flutter-apk/app-${{ steps.flavor.outputs.value }}-release.apk
          if-no-files-found: error
